# TheExchange 2.0.0

2.0.0 是一次不兼容旧协议的版本更新。所有互联服务器必须一起升级；本版本会明确拒绝 v1 连接和请求。

## 主要变化

- 玩家私有共享空间以 UUID 作为唯一身份，角色名解析交由 Minecraft 认证逻辑处理；仅在玩家主动创建时建立档案。
- 玩家仓库令牌只保存在内存中，具有滑动过期时间，服务器重启后失效。
- v2 快照携带远端槽位最大堆叠数，缓存只用于本地规划，最终限制仍以远端仓库为准。
- 每个打开的菜单按操作资源控制异步并发：冲突操作会被拒绝，不同来源和目标的操作可以并行完成。
- 远端变更采用 V2 未关闭事务协议；结果、结算和关闭均需确认，网络超时或断线只进入恢复，不会提前返还并造成重复物品。
- 仅将异常中断或正常关服时尚未关闭的事务写入恢复日志，完成后立即删除；令牌不会持久化，失去原投递目标的恢复物品进入内置结算仓。
- 漏斗操作保持异步，不阻塞 Minecraft 主线程；单个漏斗有请求在途时拒绝其新请求，并通过非空槽位轮询改善多个漏斗之间的公平性。
- 玩家操作和漏斗操作无法原路返还物品时，会将物品掉落到世界中，避免物品错误合并或丢失。
- 支持 Fabric 1.21.11、26.1.2 和 26.2。

## 升级说明

升级前请停止所有互联服务器并备份配置与数据库，然后在所有节点上替换 Mod。不要让 1.x 与 2.0.0 节点混合运行。
